# ADR 0005：市场分片、租赁和围栏

- 状态：已接受
- 日期：2026-08-14

## 决定

`market_id` 标识一个逻辑分片。持久化路由分配其 Kafka 分区和路由
时代。 Kafka组所有权是正常的协调者，但每个权威匹配者也写
检查 PostgreSQL 租约并单调增加防护令牌。

## 恢复

重新平衡会暂停输入、记录检查点并转移所有权。一位新作家获得了
更高的令牌，加载校验和验证的快照，并重放`domain_events`。陈旧的代币不能
犯罪。
