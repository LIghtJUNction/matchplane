# ADR 0002：数据授权和交付

- 状态：已接受
- 日期：2026-08-14

## 决定

PostgreSQL 是权威的。数据库写入和传出消息使用事务发件箱。
PostgreSQL 使用者在同一事务中使用唯一的收件箱行作为其效果。卡夫卡是一个
至少一次持久总线。 Valkey 仅存储派生的、受序列保护的投影。

## 结果

中继崩溃可以重复发布而不重复事实。 Valkey 丢失会影响延迟，
不正确，并通过重播修复。
