# ADR 0001：模块化 Rust monorepo

- 状态：已接受
- 日期：2026-08-14

## 决定

使用一个 Cargo 工作区与独立于基础设施的库箱并独立
可部署的服务二进制文件。在证明所有权或发布节奏之前不要拆分存储库
边界需要它。

## 结果

跨板条箱的更改仍然是原子的，并且 CI 可以强制执行一项 lint 和依赖基线。服务
图像和 Linux 包仍然可以包含单独的二进制文件。
