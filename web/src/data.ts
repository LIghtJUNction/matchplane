/**
 * Production data is owned by the root API and the active subplatform manifest.
 *
 * This module intentionally contains no inventory, gateway, revenue, or activity fixtures.
 * Tests should provide their own fixtures so a deployment never advertises fabricated supply.
 */
export const recommendations = [] as const;
