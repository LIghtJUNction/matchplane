# 点击验证清单（CLICK-VERIFY-CHECKLIST）

> 环境：`cd D:\matchplane && bun run --cwd web dev` → 打开 http://127.0.0.1:4173
> 后端依赖（pg/kafka/valkey）未起时属预期：商品区显示诚实空态/失败态，页面不崩。
> 逐条点，通过打 ✅；失败把现象记在条目后。
>
> 已由 EvoX 通过浏览器通道实际点击验证的条目标注了「已验证」。

## A. 公共首页（商品先行，AI 次之）

- [x] A1. 打开 http://127.0.0.1:4173，第一屏是「MATCHPLANE 商城 / 发现适合你的商品」商品区，不是聊天框 —— 已验证
- [x] A2. 后端未起时，商品区显示「商品货架读取失败 / 选货员仍然可用，可以直接描述你的需要」并提供「重新读取商品」按钮，页面不崩 —— 已验证
- [x] A3. 右侧「店铺」区显示「暂时还没有营业中的店铺。」 —— 已验证
- [x] A4. 配色为米白底 + 墨绿/近黑 + 陶土红提示，**无科技蓝** —— 已验证（截图确认）
- [x] A5. 右下角有「问选货员」入口；点击后展开浮层，标注「可拖动、缩放和收纳」，带「收纳选货员」「关闭选货员」按钮与「商品搜索」区域 —— 已验证
- [ ] A6. 顶部「浏览店铺」链接可跳转店铺目录
- [ ] A7. 顶部配色切换（选择配色：墨色）与暗色切换可用，切换后仍无蓝色
- [ ] A8. EN 语言切换后首页文案变英文

## B. AI 选货员（工具调用：主动提问 + 展示车辆）

- [x] B1. 浮层内有「想找什么？」输入区 —— 已验证
- [ ] B2. 输入「预算 10 万左右买家用车」发送：AI 未配置时显示诚实错误提示，不崩、不编造商品
- [ ] B3. 若已配置 AI：追问以**选项卡**形式出现，可点选，不是一大段文字
- [ ] B4. 推荐结果以**商品卡**出现在会话里（图、名、价），点卡片能打开商品详情
- [ ] B5. 会话可清空；浮层可拖动、收纳、关闭后可重新打开

## C. 登录与联系交换（只用验证过的联系方式）

- [ ] C1. 点右上角「登录」：邮箱登录可用
- [ ] C2. 登录后打开任一商品详情 → 联系店家：显示 StoreContactConsentCard
- [ ] C3. 账号**没有**已验证邮箱/手机时：显示「没有已验证的邮箱或手机 / 请先在账号中绑定并验证联系方式；平台不支持手填」——**没有任何手填输入框**
- [ ] C4. 有已验证邮箱时：卡片列出「已验证邮箱 xxx@xxx」，并注明「同意后，只有以下已验证绑定可在店员也同意后交换。AI 不能修改这些内容，也不能替你同意」
- [ ] C5. 未双方同意前看不到对方联系方式

## D. 管理后台「登录方式」面板（本轮新增）

- [ ] D1. 访问 `/?role=platform`（需 root 管理员账号）进入平台工作台
- [ ] D2. 切到「品牌与设置」：能看到「登录方式」面板（eyebrow：账号接入）
- [ ] D3. 面板列出：邮箱、手机号 OTP、微信 OAuth、Google、QQ、支付宝、实名认证，各自显示 已配置/未配置
- [ ] D4. 微信项显示回调地址 `/api/auth/callback/wechat` 与所需 env 键名
- [ ] D5. 手机 OTP 项显示「MATCHPLANE_SMS_PROVIDER_URL 指向短信网关 POST 接口…」提示
- [ ] D6. 未登录 curl `/api/admin/auth-providers` 返回 403 JSON

## E. 质量门

- [ ] E1. `bun run --cwd web check` 全绿（测试 + typecheck + build）
- [ ] E2. DevTools 375px 宽下首页单列排布、选货员浮层可用
- [ ] E3. 键盘 Tab 可达所有主操作，焦点框非蓝色

---

失败时把「条目号 + 现象」发回来即可修。
